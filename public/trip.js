class Trip {
  constructor (city, image, departureDate, returnDate, tripId) {
    this.city = city
    this.image = image
    this.departureDate = departureDate
    this.returnDate = returnDate
    this.tripId = tripId
 }
}

module.exports = Trip
